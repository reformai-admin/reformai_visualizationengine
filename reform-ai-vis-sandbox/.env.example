# URL of the running visualization service backend.
# Default is http://localhost:8080 (the service's default port).
# Only change this if your backend is running on a different host/port.
VITE_API_URL=http://localhost:8080
